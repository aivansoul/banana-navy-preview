---
type Props = { data: unknown | unknown[] };
const { data } = Astro.props;
const blocks = Array.isArray(data) ? data : [data];
---
{blocks.map((b) => (
  <script type="application/ld+json" set:html={JSON.stringify(b)} />
))}
